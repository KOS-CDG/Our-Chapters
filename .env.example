# Cloudinary cloud name used to build delivery URLs — see src/lib/cloudinary.ts.
# Without this set, images fall back to placeholder photos so the app still runs in dev.
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
